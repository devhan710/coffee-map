# 아바라 MVP 기획 명세서 part 2

part 1([MVP-명세서.md](MVP-명세서.md))의 Must와 대부분의 Could는 이미 있다. 이번 범위는 **가입한 사람만 쓰는 취향**과, 그걸 받치는 **Firebase + Next.js**다.

지도·검색·위치는 계속 비로그인으로 된다. 하은이는 가입하면 끈다. 로그인은 찜이나 내 음료를 눌렀을 때만 묻는다.

---

## 1. 이미 된 것

다시 만들지 않는다.

| 기능 | 한 줄 |
|---|---|
| 내 위치 | 버튼을 눌렀을 때만 권한을 묻고, 지도를 내 자리로 옮긴다 |
| 카페 이름 검색 | 집 이름을 치면 대표 메뉴가 나온다. 없으면 “없는 집” |
| 메뉴로 집 걸러 보기 | 아메리카노·핸드드립·라떼·아인슈페너·기타 |
| 집 화면 | 바텀시트. 대표 1개 + 후보 2개 |
| 집 전용 링크 | `/cafes/[id]`를 열면 그 집이 열린다 |
| 최근 본 카페 | 아까 본 집을 다시 연다 |
| 메뉴 이름 복사 | 메뉴 이름을 복사한다 |
| 공유 | “여기 이거 시키래”와 집 링크. Web Share / 클립보드 |
| 중구 밖 안내 | “이 동네는 추후 개방 예정” 한 줄. 목록은 비우지 않는다 |
| 네이버로 열기 | part 1 Won't인데 이미 있다. 유지하고 확장하지 않는다 |

카페 목록은 로컬 JSON 30곳이다.

---

## 2. 기능 정의 (MoSCoW)

part 1의 “가입 없이 사용”은 **게스트 기본**으로 남긴다. 가입은 부가 기능만 연다.

| 기능명 | 한 줄 설명 | 우선순위 | 이유 |
|---|---|---|---|
| **Google 로그인 / 로그아웃** | 켜자마자 묻지 않는다. 찜·내 음료에서만 묻는다 | **Must** | 취향을 계정에 붙이려면 사람이 있어야 한다 |
| **좋아하는 음료** | 아메리카노·핸드드립·라떼·아인슈페너·기타 중 내가 시킬 것을 체크한다 | **Must** | 가입해서 하고 싶은 일이다. 나중에 그 집을 강조한다 |
| **취향 강조** | 필터로 집을 지우지 않는다. 맞는 집만 지도에서 강조하고, 검색에 “내 음료 집”을 둔다 | **Must** | 집을 숨기면 빈 지도가 된다 |
| **카페 찜** | 집 화면에서 하트를 누른다. 검색에 “찜한 집”이 있다. 비로그인이면 로그인을 유도한다 | **Must** | 다시 올 집을 계정에 남긴다 |
| 서버에서 카페 읽기 | 카페 목록을 서버가 읽어서 화면에 넘긴다. 여유 있으면 Firestore로 옮긴다 | Should | Next.js를 껍데기가 아니라 데이터 입구로 쓴다 |
| 찜·음료 저장 | 서버에서 로그인인지 확인한 뒤 저장한다 | Should | 쓰기가 브라우저에만 있으면 Next를 안 쓰는 것과 같다 |
| 지도 스크립트 | 네이버 지도를 Next 방식으로 로드한다 | Could | 동작은 같고 껍데기만 맞춘다 |

음료 id는 지금 필터와 같다. `americano`, `drip`, `latte`, `einspanner`, `other`.

---

## 3. 사용자 흐름

기본 길은 part 1과 같다. `단톡 링크 → 지도 → 집`. 가입은 그 위에 얹는다.

| 화면 | 하는 일 | 다음으로 가는 때 |
|---|---|---|
| 1. 지도 | 비로그인으로 중구(또는 내 자리)와 집을 본다. 로그인이면 내 음료에 맞는 집이 강조된다 | 집을 누르면 집 화면. 검색을 치면 검색. 내 음료를 누르면 로그인 또는 체크 |
| 2. 검색 | 집 이름을 친다. 로그인이면 찜한 집·내 음료 집이 위에 있다 | 집이 있으면 집 화면. 없으면 “없는 집” |
| 3. 집 | 대표 메뉴를 본다. 하트를 누른다 | 비로그인이면 로그인을 묻는다. 닫으면 지도 |
| 4. 로그인 | Google로 들어온다 | 들어오면 방금 하려던 찜 또는 음료 체크로 돌아간다 |

켜자마자 로그인 창을 띄우지 않는다. 거절해도 지도와 검색은 그대로다.

---

## 4. 데이터

유저만 Firestore에 둔다. 카페는 당분간 JSON이다.

```
users/{uid}
  preferredDrinkIds: string[]
  updatedAt

users/{uid}/favorites/{cafeId}
  cafeId
  createdAt
```

규칙은 자기 `users/{uid}`만 읽고 쓴다. 카페를 Firestore로 옮기면 공개 읽기, 클라이언트 쓰기는 막는다.

Firestore 위치는 `asia-northeast3`. 구현 전에 프로젝트 ID를 정한다.

---

## 5. 이번엔 안 할 것

part 1 Should 중 구현 단계에서 뺀 것은 다시 넣지 않는다.

| 기능명 | 한 줄 설명 | 이유 |
|---|---|---|
| 근거 숫자 | 후기에 몇 번 나왔는지 보여 준다 | 구현 단계에서 뺐다 |
| 지도 메뉴 사진 | 핀 대신 메뉴 사진이 지도에 있다 | 구현 단계에서 뺐다 |
| 카카오 SDK 공유 | 카카오 앱으로 “여기 이거 시키래”를 보낸다 | 구현 단계에서 뺐다. Web Share로 충분하다 |
| 블로그 추출 / 동별 시드 | 핀을 누르면 후기에서 메뉴를 뽑는다 | 기다리면 끈다. 메뉴는 미리 넣은 목록 |
| 메뉴 투표 | 손님이 대표를 고른다 | part 1에서 미룬 것 |
| 사장님 시그니처 | 사장이 우리 집 메뉴를 올린다 | part 1에서 미룬 것 |
| 켜자마자 로그인 | 첫 화면에서 가입을 묻는다 | 묻는 순간 끈다 |
| 이메일 가입·알림 | 메일로 묶고 푸시를 보낸다 | Google만 한다 |

---

## 6. 구현 순서

1. Firebase 프로젝트, Google 로그인, Firestore, 규칙
2. 게스트 지도 유지. 찜·내 음료에서만 로그인
3. 카페 찜, 검색에 찜한 집
4. 좋아하는 음료, 지도에서 강조
5. 여유 있으면 카페 목록을 Firestore로
